import { __ } from '@wordpress/i18n';
import { chartBar } from '@wordpress/icons';

const widget = {
	name: 'underway/habit-creator',
	title: __( 'Habit Creator', 'underway' ),
	icon: chartBar,
	attributes: [],
	example: {},
};

export default widget;
