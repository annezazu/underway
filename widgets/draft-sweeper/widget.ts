import { __ } from '@wordpress/i18n';
import { edit } from '@wordpress/icons';

const widget = {
	name: 'underway/draft-sweeper',
	title: __( 'Draft Sweeper', 'underway' ),
	icon: edit,
	attributes: [],
	example: {},
};

export default widget;
